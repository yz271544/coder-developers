#plugin_cache_dir  = "/providers"
#disable_checkpoint = true

provider_installation {
  filesystem_mirror {
    path    = "/providers"
    include = ["*/*"]
  }
}

provider_cache {
  central_directory = "/providers"
  mode              = "shared"
}
